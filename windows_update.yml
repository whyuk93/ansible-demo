---

- hosts: DC1
  become: yes
  become_user: ops-admin@test.com
  tasks:
    - name: Windows Update
      win_updates:
        category_names:
          - SecurityUpdates
          - CriticalUpdates
          - UpdateRollups
        reboot: "{{ isreboot }}"
        reboot_timeout: "{{ timeout }}"